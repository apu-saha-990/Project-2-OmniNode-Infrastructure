# =============================================================
# OmniNode — ConfigMap
# Non-sensitive configuration for all pods
# =============================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: omninode-config
  namespace: omninode
data:
  BTC_RPC_USER: "omninode_btc"
  BTC_RPC_PORT: "8332"
  BTC_P2P_PORT: "8333"
  ETH_HTTP_PORT: "8545"
  ETH_WS_PORT: "8546"
  ETH_P2P_PORT: "30303"
  ETH_METRICS_PORT: "6060"
  PROMETHEUS_PORT: "9090"
  GRAFANA_PORT: "3000"
  ALERTMANAGER_PORT: "9093"
