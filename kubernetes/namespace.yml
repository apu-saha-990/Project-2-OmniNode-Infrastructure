# =============================================================
# OmniNode — Kubernetes Namespace
# =============================================================
apiVersion: v1
kind: Namespace
metadata:
  name: omninode
  labels:
    app: omninode
    environment: production
